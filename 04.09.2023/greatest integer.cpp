#include<stdio.h>
int main()
{
    int a,b,c,x,y,z;
    printf("Enter three numbers");
    scanf("%d %d %d", &a, &b, &c);
    if(a>b)
    x=a;
    else
    x=b;
    if (b>c)
    y=b;
    else
    y=c;
    if(x,y)
    z=x;
    else
    z=y;
    printf("The greater integer is %d",z);
    return 0;
}
