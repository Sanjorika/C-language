#include<stdio.h>
int main()
{
  int i=0,l=0;
  char str[1000];
  printf("Enter the string");
  scanf("%s",str1);
  while(str[i]!='\0')
  {
    l++;
    i++;
  }
  printf("%d is the length of the given string",l);
  return 0;
}
